<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!--
        Postgres, not sqlite: this project commits to PostgreSQL + PostGIS as
        the only database engine (see docs/DATABASE_SCHEMA.md), and migrations
        use Postgres-specific syntax (CHECK constraints, later PostGIS
        geography columns) that sqlite can't run. CI provisions the same
        `tow_platform_testing` database as a service container.
        -->
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_HOST" value="127.0.0.1"/>
        <env name="DB_PORT" value="5432"/>
        <env name="DB_DATABASE" value="tow_platform_testing"/>
        <env name="DB_USERNAME" value="postgres"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
        <!-- Matches the test HTTP client's default REMOTE_ADDR (127.0.0.1
             — see vendor/symfony/http-foundation/Request.php), so
             tests/Feature/Security/TrustedProxiesTest.php can exercise
             real X-Forwarded-* handling instead of it always being off
             in the test environment. -->
        <env name="TRUSTED_PROXIES" value="127.0.0.1"/>
    </php>
</phpunit>
